/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true, // Enforces best practices for React, helps catch potential issues
    swcMinify: true, // Enables the faster SWC compiler for minification
  };
  
  export default nextConfig;
  