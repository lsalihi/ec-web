"use client";
import React, { useState } from 'react';

const AddChargingPoint = () => {
  const [formData, setFormData] = useState({
    name: '',
    type: '',
    status: '',
    energySource: '',
    location: '',
    connectors: '',
    moreDetails: '',
    owner: '',
  });

  const handleChange = (e: React.ChangeEvent<HTMLInputElement | HTMLTextAreaElement>) => {
    const { name, value } = e.target;
    setFormData({
      ...formData,
      [name]: value,
    });
  };

  const handleSubmit = (e: React.FormEvent) => {
    e.preventDefault();
    // Handle form submission logic here
    console.log('Form data submitted:', formData);
  };

  return (
    <div className="container mx-auto p-4">
      {/* Header with Navigation Links */}
      <header className="mb-4">
        <nav className="flex space-x-4">
          <a href="#information" className="text-blue-500 hover:underline">Information</a>
          <a href="#location" className="text-blue-500 hover:underline">Location</a>
          <a href="#connectors" className="text-blue-500 hover:underline">Connectors</a>
          <a href="#more-details" className="text-blue-500 hover:underline">More Details</a>
          <a href="#owner" className="text-blue-500 hover:underline">Owner</a>
        </nav>
      </header>

      <h1 className="text-2xl font-bold mb-4">Add New Charging Point</h1>
      <form onSubmit={handleSubmit}>
        {/* Information Section */}
        <div id="information" className="mb-4">
          <h2 className="text-xl font-semibold mb-2">Information</h2>
          <div className="mb-2">
            <label className="block mb-1">Charging Station Name</label>
            <input
              type="text"
              name="name"
              value={formData.name}
              onChange={handleChange}
              className="w-full p-2 border border-gray-300 rounded-md"
            />
          </div>
          <div className="mb-2">
            <label className="block mb-1">Type</label>
            <input
              type="text"
              name="type"
              value={formData.type}
              onChange={handleChange}
              className="w-full p-2 border border-gray-300 rounded-md"
            />
          </div>
          <div className="mb-2">
            <label className="block mb-1">Status</label>
            <input
              type="text"
              name="status"
              value={formData.status}
              onChange={handleChange}
              className="w-full p-2 border border-gray-300 rounded-md"
            />
          </div>
          <div className="mb-2">
            <label className="block mb-1">Energy Source</label>
            <input
              type="text"
              name="energySource"
              value={formData.energySource}
              onChange={handleChange}
              className="w-full p-2 border border-gray-300 rounded-md"
            />
          </div>
        </div>

        {/* Location Section */}
        <div id="location" className="mb-4">
          <h2 className="text-xl font-semibold mb-2">Location</h2>
          <div className="mb-2">
            <label className="block mb-1">Location</label>
            <input
              type="text"
              name="location"
              value={formData.location}
              onChange={handleChange}
              className="w-full p-2 border border-gray-300 rounded-md"
            />
          </div>
        </div>

        {/* Connectors Section */}
        <div id="connectors" className="mb-4">
          <h2 className="text-xl font-semibold mb-2">Connectors</h2>
          <div className="mb-2">
            <label className="block mb-1">Connectors</label>
            <input
              type="text"
              name="connectors"
              value={formData.connectors}
              onChange={handleChange}
              className="w-full p-2 border border-gray-300 rounded-md"
            />
          </div>
        </div>

        {/* More Details Section */}
        <div id="more-details" className="mb-4">
          <h2 className="text-xl font-semibold mb-2">More Details</h2>
          <div className="mb-2">
            <label className="block mb-1">More Details</label>
            <textarea
              name="moreDetails"
              value={formData.moreDetails}
              onChange={handleChange}
              className="w-full p-2 border border-gray-300 rounded-md"
            />
          </div>
        </div>

        {/* Owner Section */}
        <div id="owner" className="mb-4">
          <h2 className="text-xl font-semibold mb-2">Owner</h2>
          <div className="mb-2">
            <label className="block mb-1">Owner</label>
            <input
              type="text"
              name="owner"
              value={formData.owner}
              onChange={handleChange}
              className="w-full p-2 border border-gray-300 rounded-md"
            />
          </div>
        </div>

        <button type="submit" className="p-2 bg-blue-500 text-white rounded-md">
          Submit
        </button>
      </form>
    </div>
  );
};

export default AddChargingPoint;